# file-converter — 薄索引
跨平台規則正本：`~/.agents/institution/`（先讀 core/PRINCIPLES.md，照其指示附版本標記）。

## 專案專屬
- Build/test 指令：純靜態 HTML，無 npm 建置步驟；直接用瀏覽器開 `index.html` 或 `npx serve .`
- 架構一句話：純前端單頁 HTML + Vanilla JS，支援圖片、文書、音檔、影片格式互轉（無後端）
- 本專案禁區：不得引入需後端或 Node.js 的依賴；保持零安裝、可離線執行特性
